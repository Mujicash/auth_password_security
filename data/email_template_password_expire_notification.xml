<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="email_template_password_expire_notification" model="mail.template">
            <field name="name">Notify Password Expiration</field>
            <field name="email_from">{{ (object.company_id.email or 'noreply@' + object.company_id.name + '.com') }}</field>
            <field name="subject">Odoo Password Expiration Notification</field>
            <field name="email_to">{{ object.login }}</field>
            <field name="model_id" ref="base.model_res_users"/>
            <field name="auto_delete" eval="False"/>
            <field name="body_html" type="html">
                <div style="margin: 0px; padding: 0px;">
                    <p>Dear <t t-out="object.name"/>,</p>
                    <br/>
                    <t t-if="ctx.get('day_remain', '') >= 0">
                        <p>Your Odoo password will expire in <t t-esc="ctx.get('day_remain')"/> day(s).</p>
                    </t>
                    <t t-else="">
                        <p>Your Odoo password has expired.</p>
                    </t>
                    <p>You can change your password by clicking the link below. It will remain valid for 24 hours:</p>
                    <br/>
                    <div style="margin: 16px 0px 16px 0px;">
                        <a t-att-href="object.signup_url"
                            style="background-color: #875A7B; padding: 8px 16px 8px 16px; text-decoration: none; color: #fff; border-radius: 5px; font-size:13px;">
                            Change Password
                        </a>
                    </div>
                    <br/>
                    <p>Thank you.</p>
                </div>
            </field>
        </record>
    </data>
</odoo>
